<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayoutNavegacionLateralAdministrador"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:openDrawer="start"
    android:fitsSystemWindows="true"
    tools:context=".administrador.AdministradorActivity">

    <!-- Contenedor principal usando ConstraintLayout -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Toolbar en la parte superior -->
        <include
            android:id="@+id/toolbarAdministrador"
            layout="@layout/activity_toolbar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Contenedor para los fragmentos -->
        <FrameLayout
            android:id="@+id/frameLayoutContenedorFragmentoAdmin"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@+id/toolbarAdministrador"
            app:layout_constraintBottom_toTopOf="@+id/bottomNavigationNavegacionAdministrador"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- BottomNavigationView en la parte inferior -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigationNavegacionAdministrador"
            style="@style/navegacionInferior"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/navegacion_inferior_administrador" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- NavigationView para el Drawer -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationViewNavegacionLateral"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:itemIconTint="@color/color_principal"
        app:itemTextColor="@color/color_texto"
        app:headerLayout="@layout/cabecera_navegacion_lateral"
        app:menu="@menu/navegacion_lateral" />

</androidx.drawerlayout.widget.DrawerLayout>

